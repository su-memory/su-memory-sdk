# 结构化需求（8 要素必填，禁止一句话需求）

## 基本信息
- 风险等级：L0 / L1 / L2（由 `scripts/ai-verify/risk-classify.sh` 定级为准，此处人工预估）
- 目标分支：
- 目标 PR：
- 数据库迁移：无 / 有（说明）
- 新增依赖：无 / 有（名称+版本+用途）

## 1. 业务目标
> 本次改动要解决什么问题、达到什么效果（一句话 + 背景说明）。

## 2. 入参
| 参数 | 类型 | 必填 | 校验规则 | 来源 |
| --- | --- | --- | --- | --- |

## 3. 出参（返回结构）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
> 必须遵循项目既有统一返回结构，禁止自创格式。

## 4. 依赖
- 内部依赖（调用的模块/服务/表）：
- 外部依赖（第三方服务/API）：

## 5. 边界条件
- 空值/缺失字段：
- 重复提交/幂等：
- 并发/竞态：
- 权限/越权：

## 6. 异常处理
| 异常场景 | 错误码/响应 | 日志要求 |
| --- | --- | --- |
> 禁止裸 except、禁止吞异常；异常必须受控处理。

## 7. 性能要求
- 预估 QPS / 数据量：
- 禁止 N+1 / 循环内查库 / 全量返回：

## 8. 测试要求
| 用例 | 输入 | 期望输出 | 类型（正常/边界/异常/空值） |
| --- | --- | --- | --- |

## 人工评审结论
- [ ] 架构评审通过（评审人：____）
- [ ] 需求范围确认（无扩展需求）
