# su-memory 企业级AI记忆平台
## 产品介绍 · 2026

---

## 一、产品定位

**su-memory** 是企业级AI长期记忆中台，为私有化大模型提供人类级别的记忆能力。

让AI真正"记住"企业和用户的长期信息，实现多轮对话的连贯推理、个性化服务和高置信度决策。

---

## 二、核心价值

```
┌─────────────────────────────────────────────────────────────┐
│                     su-memory 核心价值                        │
├─────────────────────────────────────────────────────────────┤
│  ✅ 记得更多    比GPT-4o记住多40%的长期信息                    │
│  ✅ 记得更准    冲突检测准确率97.8%，告别矛盾回答               │
│  ✅ 响应更快    延迟低于200ms，企业级流畅体验                   │
│  ✅ 用得更久    医用级稳定性，7×24小时运行                     │
│  ✅ 私有部署    数据不出企业，满足合规要求                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、效果对比

### 3.1 记忆能力对比

| 指标 | GPT-4o | Mem0 | Zep | su-memory |
|------|--------|------|-----|-----------|
| 长期信息记住率 | 60.2% | 72.1% | 68.3% | **96.7%** |
| 多轮对话连贯性 | 75% | 82% | 80% | **94%** |
| 冲突信息检测 | 无法处理 | 75% | 70% | **97.8%** |
| 遗忘率（30天后） | 80% | 35% | 40% | **<5%** |
| 上下文窗口利用率 | 100% | 60% | 55% | **95%** |

*数据来源：LongMemEval、LoCoMo公开基准测试*

### 3.2 企业场景效果

| 场景 | 传统方案 | su-memory |
|------|---------|-----------|
| 客服多轮对话 | 每轮重述背景 | 全程记住，响应精准 |
| 医疗问诊 | 病史遗漏风险 | 完整记忆，诊断辅助 |
| 法律咨询 | 文件重复查阅 | 全量记忆，快速引用 |
| 金融投顾 | 客户偏好丢失 | 深度理解，个性化服务 |
| 企业私域AI | 记忆混乱矛盾 | 结构化记忆，一致性高 |

---

## 四、核心功能

### 4.1 四大记忆能力

| 功能 | 说明 | 效果 |
|------|------|------|
| **持久记忆** | 跨会话、跨时间记住关键信息 | 30天记忆完整率>95% |
| **智能检索** | 多维度精准召回所需记忆 | 召回率>95%，延迟<200ms |
| **冲突消解** | 自动检测并解决记忆矛盾 | 冲突检测率>97% |
| **主动遗忘** | 低价值记忆自动归档 | 存储效率提升80% |

### 4.2 企业级特性

| 特性 | 说明 |
|------|------|
| **多租户隔离** | 企业间数据完全独立，互不可见 |
| **私有化部署** | Docker镜像一键部署，数据不出本地 |
| **毫秒级延迟** | P95<200ms，企业级响应体验 |
| **水平扩展** | 支持10万+用户并发 |
| **合规认证** | 支持等保三级、医疗合规 |

---

## 五、技术架构（黑盒封装）

```
┌─────────────────────────────────────────────────────────────┐
│                        su-memory                             │
│                   企业级AI记忆中台                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐   │
│   │ 接入网关     │  →  │  记忆引擎   │  →  │ 存储引擎    │   │
│   │             │     │             │     │             │   │
│   │ · 鉴权       │     │ · 持久记忆   │     │ · 向量库    │   │
│   │ · 租户隔离   │     │ · 智能检索   │     │ · 时序库    │   │
│   │ · 流量控制   │     │ · 冲突消解   │     │             │   │
│   └─────────────┘     └─────────────┘     └─────────────┘   │
│                                                              │
│   ┌─────────────────────────────────────────────────────┐   │
│   │              全版本统一API（OpenAI兼容）              │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**部署方式**：单一Docker镜像，对外零依赖，开箱即用

---

## 六、部署要求

| 项目 | 要求 |
|------|------|
| **部署环境** | Linux x86_64，Docker 20.10+ |
| **内存** | 最低8GB，推荐16GB+ |
| **存储** | 最低50GB SSD |
| **GPU** | 可选（加速向量检索） |
| **网络** | 千兆内网 |
| **大模型** | 兼容Ollama/vLLM/Qwen/通义等所有OpenAI兼容接口 |

---

## 七、接口示例

```bash
# 写入记忆
curl -X POST http://localhost:8000/v1/memory/add \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"user_id": "user_001", "content": "患者有高血压病史"}'

# 检索记忆
curl -X POST http://localhost:8000/v1/memory/query \
  -H "Authorization: Bearer $API_KEY" \
  -d '{"user_id": "user_001", "query": "患者有什么慢性病"}'

# 带记忆的对话
curl -X POST http://localhost:8000/v1/chat/completions \
  -H "Authorization: Bearer $API_KEY" \
  -d '{
    "model": "qwen",
    "messages": [{"role": "user", "content": "这个患者该怎么用药"}]
  }'
```

---

## 八、服务支持

| 项目 | 说明 |
|------|------|
| **交付形式** | Docker镜像 + 部署文档 |
| **技术支持** | 7×24小时响应 |
| **版本升级** | 每年4次版本更新 |
| **定制服务** | 行业模板、行业知识库集成（另议） |

---

## 九、联系我们

**商用洽谈**：[联系方式待定]

**部署咨询**：[联系方式待定]
