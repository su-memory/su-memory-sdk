# AI 编程工程化基线（AI 宪法）

> 所有 AI 编码助手的最高约束，优先级仅次于人类当次显式指令。
> 铁律：**人类定义架构、规则、边界、安全；AI 只做标准化执行；所有 AI 输出必须经校验、测试、质检、人工终审。**

## 0. 总则
1. 代码必须工程化、可维护、可测试、可追溯。
2. 禁止 Vibe Coding：禁止模糊需求、无脑 Accept、无审核合入。
3. 业务代码必须：结构化需求 + 架构评审 + Skill 复用 + 单元测试 + 质检 + 人工终审。

## 1. 技术栈约束
| 层级 | 技术 |
| --- | --- |
| 语言 | Python ≥ 3.11 |
| Web | FastAPI |
| 校验 | Pydantic v2 |
| HTTP | httpx |
| LLM | openai / anthropic SDK（按项目实际版本） |
| 异步 | asyncio |
| 测试 | pytest + pytest-asyncio |
| Lint | ruff + mypy |
- 禁止私自引入新依赖；变更需记录原因、用途、版本，更新 requirements*.txt / pyproject.toml。

## 2. 目录分层（严格分层，禁止跨层）
```
api/ 或 routers/   路由层：参数解析 + 调 service + 统一响应；禁止业务逻辑
services/          业务层：业务逻辑、事务、流程控制唯一归属
models/            数据层：ORM 模型，数据库读写只在此
schemas/           DTO：Pydantic 入参/出参
core/ 或 common/   基础设施：config/security/exceptions/logging/cache
tests/             单元 + API 测试
```
铁律：路由/组件不写业务、不直接写库；Service 不返回 HTTP 异常；禁止跨层调用。

## 3. 命名规范
- Python: 变量/函数 snake_case；类 PascalCase；常量 UPPER_SNAKE。
- 前端: 组件 PascalCase；变量/函数 camelCase；常量 UPPER_SNAKE。
- 布尔用 is/has/can 前缀。禁止拼音、歧义简写。

## 4. 代码强制规则
1. 单一职责，单函数 ≤ 80 行，嵌套 ≤ 3 层。
2. 禁止魔法值，抽常量/枚举。
3. 所有入参校验（Pydantic / zod）。
4. 所有异常受控处理，禁止裸 except/catch / 吞异常。
5. 禁止裸 SQL 拼接，参数化 / ORM。
6. 禁止硬编码密钥/配置，走 .env。
7. 禁止 print/console.log 进生产，用 logging / 日志库。

## 5. 统一响应与异常
- 统一返回结构（按项目既有格式，如 `{code,msg,data}` 或 `{success,data,error}`），禁止自创。
- 异常集中定义（core/exceptions 或统一 error 模块），禁止散落抛原生异常。

## 6. 安全规范
1. 所有入参校验；写接口鉴权。
2. SQL 参数化，防注入。
3. 敏感字段（密钥/token/手机号/身份证）走环境变量，禁止硬编码、禁止明文日志。
4. 外部 LLM/HTTP 调用必须有超时与重试上限。

## 7. 性能规范
1. 禁止 N+1；禁止循环内查库/发 HTTP。
2. 列表分页，禁止全量返回。
3. 大批量操作分批/异步。

## 8. 测试规范
1. 新增逻辑必须配套测试（pytest / vitest）。
2. 覆盖正常/边界/异常/空值。
3. 测试全绿才可合入。

## 9. AI 专属禁止清单
1. 禁止擅自新增需求、扩展功能、改接口结构。
2. 禁止擅自改目录结构/分层。
3. 禁止隐藏逻辑、隐性重试、隐性兜底。
4. 禁止自创返回格式、自创异常体系。
5. 禁止提交未跑测试的代码。

## 元规则
- 本文件与 Skill 冲突时以本文件为准；与现有代码规范冲突时以现有代码为准。
- 用户口头需求与本文件冲突时，AI 必须先指出并要求结构化需求，不得擅自降级执行。
